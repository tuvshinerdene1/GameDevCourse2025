# Game.gd  (attached to the root node of your game, e.g. Node2D named "Game")
extends Node2D

# @export var first_room: PackedScene # drag Level1.tscn here
# @onready var room_manager: Node = $Room_manager # child node with RoomManager.gd

# func _ready() -> void:
# 	if first_room:
# 		room_manager.start_level(first_room)
# 	else:
# 		push_error("Game: No first_room assigned!")
